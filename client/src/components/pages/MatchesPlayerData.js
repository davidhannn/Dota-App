import React from "react";

const MatchesPlayerData = ({ players }) => {
  //   const keys = players && Object.keys(players);
  console.log(players);
  //   for (let i = 0; i < keys.length; i++) {
  //     const key = keys[i];

  //     console.log(key, players[key]);
  //   }

  return <div>{}</div>;
};

export default MatchesPlayerData;
